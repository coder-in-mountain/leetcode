class RecursionSolution {
    public List<String> letterCasePermutation(String S) {
        List<String> list = new ArrayList<>();
        dfs(S.toCharArray(), 0, list);
        return list;
    }
    void dfs(char[] chs, int index, List<String> list) {
        if
    }
}